apiVersion: v1
kind: Service
metadata:
  name: mailfind-frontend
  namespace: mailfind
  labels:
    app: mailfind
    component: frontend
spec:
  type: ClusterIP
  selector:
    app: mailfind
    component: frontend
  ports:
  - name: http
    port: 80
    targetPort: 80
    protocol: TCP

